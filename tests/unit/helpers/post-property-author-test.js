
import { postPropertyAuthor } from 'spiralcode-frontend/helpers/post-property-author';
import { module, test } from 'qunit';

module('Unit | Helper | post property author');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postPropertyAuthor([42]);
  assert.ok(result);
});

